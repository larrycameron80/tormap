Needed packages:
1. pygeoip -> https://github.com/appliedsec/pygeoip

Installation instructions:
1. copy html/* files to your web path
2. copy tormap_split.py along with runme.sh to a directory of your choice (BINDIR)
3. edit runme.sh and change the following variables:
	KMLDIR='/var/www/maps'
	TMPDIR='/tmp/tormap'
	BINDIR='/usr/local/bin/'
4. edit tormap_split and change the following variables:
	KMLDIR = '/var/www/maps/'
	HTMLDIR = '/var/www/'
	TMPDIR= '/tmp/tormap/'
5. edit html/tormap.js and change the kmlPATH variable:
	var kmlPATH='https://tormap.void.gr/maps/';
6. run runme.sh from crontab
